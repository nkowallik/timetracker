# timetracker
Local Job/PhD work-time tracker: timers, flexitime balance, vacation and sick days, German public holidays, CSV/JSON/PDF export. SvelteKit + SQLite, runs via Docker or as a desktop app.
